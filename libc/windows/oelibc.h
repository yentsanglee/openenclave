#ifndef _OELIBC_OELIBC_H
#define _OELIBC_OELIBC_H

int write_stdout(const char* data, size_t size);

int write_stderr(const char* data, size_t size);

void print(const char* str);

#endif /* _OELIBC_OELIBC_H */
